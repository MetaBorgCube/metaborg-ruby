module CompoundStatementTest

language Ruby

start symbol Program

test Arg: assign  [[
	[[t]] = 1;
	a = [[t]];
]] resolve #2 to #1

test Arg plus: assign operator 1 [[
	[[t]] = 1;
	a = [[t]] + 1;
]] resolve #2 to #1

test Arg plus: assign operator 2 [[
	[[t]] = 1;
	a = 2 + [[t]];
]] resolve #2 to #1

test Arg plus: assign operator 3a [[
	[[t]] = 1;
	a = [[t]] + t;
]] resolve #2 to #1

test Arg plus: assign operator 3b [[
	[[t]] = 1;
	a = t + [[t]];
]] resolve #2 to #1


test Var 't' not found [[
	a = 1;
	b = t + 1;
]] >= 1 errors

test Stmt alias: b not found [[
	alias a b;
]] >= 1 errors